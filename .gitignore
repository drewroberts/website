# Dependencies
node_modules/

# Build Output
# This is where your final static site lives. 
# It is generated by GitHub Actions, so it should never be in Git.
dist/

# Astro Cache
# This contains processed images (AVIF/WebP) and content metadata.
.astro/

# Environment Secrets
.env
.env.*
!.env.example

# Search Index (Local)
# Pagefind generates this during build; no need to track it.
/public/pagefind/
/public/_pagefind/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS Files
.DS_Store
Thumbs.db
